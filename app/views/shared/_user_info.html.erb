<%= link_to gravatar_for(current_user, size: Settings.link.index.gravatar_size), current_user %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to t(".view_profile"), current_user %>
</span>
<span>
  <%= t(".microposts", count: current_user.microposts.size) %>
</span>
