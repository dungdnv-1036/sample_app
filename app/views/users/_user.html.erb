<li>
  <%= gravatar_for user, size: Settings.link.index.gravatar_size  %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
  |  <%= link_to t("users.delete_user"), user, method: :delete, data: {confirm: t("users.confirm_delete_user")} %>
  <% end %>
</li>
